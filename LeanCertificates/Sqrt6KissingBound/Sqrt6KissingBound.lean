import Sqrt6KissingBound.Core

/-!
# A conditional Lean certificate for the `sqrt 6` kissing-number argument

The file `Core.lean` checks the numerical inequalities, the two-step induction,
and the deduction of the count bound from cap packing. The geometric theorem
identifying the cap fraction of a spherical cap, and the corresponding cap-packing
inequality for kissing configurations, are deliberately supplied as explicit
hypotheses rather than hidden as axioms.
-/
